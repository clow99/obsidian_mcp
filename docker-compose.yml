services:
  obsidian-mcp:
    build:
      context: .
    image: obsidian-mcp:local
    tty: false
    env_file:
      - .env
    volumes:
      - ${OBSIDIAN_VAULT_HOST_PATH:-./sample-vault}:${OBSIDIAN_VAULT_PATH:-/vault}
    command: ["node", "dist/server.js"]
